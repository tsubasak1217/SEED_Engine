#include "Ring.h"
#include <SEED.h>

void Ring::Draw(){
    // 円を描画
    // DrawCircle(0, 0, outerRadius_, GetColor(255, 255, 255), true);
    // DrawCircle(0, 0, innerRadius_, GetColor(0, 0, 0), true);
}